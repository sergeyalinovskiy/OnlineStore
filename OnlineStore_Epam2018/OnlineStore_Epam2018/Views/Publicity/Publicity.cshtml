@model IEnumerable<OnlineStore_Epam2018.Models.PublicityViewModel>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Text)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Picture)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Text)
        </td>
        <td>
        <img src="~/Connected Services/@item.Picture" width="45"/>
            <img src=@item.Picture width="45" />
            @*<img @item.Picture width="45" />*@
        </td>

    </tr>
}
</table>
